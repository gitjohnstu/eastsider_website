services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: eastsider
      POSTGRES_PASSWORD: eastsider
      POSTGRES_DB: eastsider
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
